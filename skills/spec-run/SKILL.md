---
name: spec-run
description: 仕様駆動開発パイプライン(spec-new→spec-spec→spec-design→spec-tasks→spec-implement→spec-test→spec-review→spec-verify→spec-retro)を通しで実行するオーケストレーター。ユーザーが「一通りやって」「パイプラインを回して」「/spec-run」と言った時に使う。
---

# spec-run — パイプライン オーケストレーター

要件定義から振り返りまでを通しで進める。各フェーズは対応する `spec-*` スキルの手順そのものに従うこと(このファイルは順序とゲートだけを定義する)。

## 全体フロー

1. `.specs/<slug>/` が未初期化なら `spec-new` を実行する。
2. `spec-spec` を実行し、`spec.md` をユーザーに提示して **確定の同意を得る**(ゲート1)。
   - 同意が得られるまで次に進まない。
3. `spec-design` を実行し、`design.md` をユーザーに提示して **確定の同意を得る**(ゲート2)。
   - 影響範囲が小さい・自明な変更の場合はユーザーの意向に応じて軽い確認で済ませてよい。
4. `spec-tasks` を実行し、`tasks.md` をユーザーに提示して **確定の同意を得る**(ゲート3)。
5. ゲート3通過後は、`spec-implement` → `spec-test` → `spec-review` → `spec-verify` を連続して実行する。ここから先は毎フェーズ確認を取らず進めてよいが、以下の場合は立ち止まってユーザーに報告・確認する:
   - `spec-review` で重大な指摘が出た場合
   - `spec-verify` で受け入れ基準の未達がある場合
   - 実装中に spec.md / design.md と矛盾する判断が必要になった場合
6. `spec-verify` が合格したら `spec-retro` を実行してパイプラインを完了する。

## 途中再開

`.specs/<slug>/STATE.md` の `current_phase` を見て、未完了のフェーズから再開する。すでに完了しているフェーズは再実行しない(ユーザーが明示的にやり直しを指示した場合を除く)。

## 注意

- 各フェーズの詳細な手順・出力フォーマットは重複させず、対応する `spec-*` スキルに委ねる。このスキルは「いつどの順で・どこで止まるか」だけを担う。
